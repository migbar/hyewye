- title "Login"

- if @user_session.errors.on_base =~ /Could not find user in our database/
  It looks like you have not created an account on HyeWye yet. You can easily do this
  by using your Twitter account.

  - form_tag account_path do
    = oauth_register_button :value => "Use my Twitter account to register with HyeWye"
  
- else
  .yui-g.middle-stroke
    - semantic_form_for @user_session, :url => user_session_path do |f|
      .yui-u.first
        = f.error_message_on(:base)
        - f.buttons :name => "I am a Twitter user", :id => "twitter" do
          = oauth_login_button :value => "Let me log in using Twitter"
      .yui-u
        - f.inputs do
          = f.input :login
          = f.input :password
          %li
            = link_to "Forgot your password?", new_password_reset_path
        
        - f.buttons do
          = image_submit_tag("/images/css/login.png", :value => "Login!")
