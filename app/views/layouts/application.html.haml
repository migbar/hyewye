!!!
%html{html_attrs}
  %head
    = title_tag
    %meta{"http-equiv"=>"Content-type", :content=>"text/html; charset=utf-8"}
    / = stylesheet_link_tag 'application'
    = stylesheet_link_tag 'hyewye'
    = yield(:head)
  %body
    #page-wrap
      #banner
        #logo
        #banner-body.clearfix
          = render :partial => "shared/header"
      #container
        #main-content
          - if show_title?
            %h2= h yield(:title)
          = render :partial => "shared/flash", :object => flash
          .clearfix
            = yield
        #footer
          = render :partial => "shared/footer"
    = GOOGLE_ANALYTICS