/ #header_background
/   = link_to(image_tag('images/header_background.jpg'), '')
/ %p
#login_bar
  = link_to "Sign up", new_account_path if !current_user
  = link_to "Log in", new_user_session_path if !current_user
  = link_to "Log out", user_session_path, :method => 'delete' if current_user

